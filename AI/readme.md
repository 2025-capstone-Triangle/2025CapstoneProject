# pyaudio_analysis

가상환경 (py < 3.11>) 생성

터미널에

.venv\Scripts\activate -> 활성화 확인

pip install pyAudioAnalysis

pip install numpy

pip install python-dotenv

pip install langchain-google-genai

pip install eyed3

pip install pydub

pip install scipy

pip install tqdm

# langchain

terminal에

pip install python-dotenv


pip install -U langchain


pip install -U langchain-google-genai

입력

이후 AI 폴더에 .env 파일 생성 후 팀장에게 API key 문의

